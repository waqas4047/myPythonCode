# bellow is syntex for tuple tuple are immutable we can not change value of tuple
tup=(True,"waqas",24)
print(tup)

# print type of a tup
print(type(tup))
# find length of items in a tuple
print(len(tup))

# to check data type of value in a tuple
print(type(tup[0]))

# we can not reset the value of items inside the tuple
# tup[0]="gergena"                                   it will give us error
# print(tup)